function createNewDoc(data, themeColor) {
    const {
      avatar_url: imgUrl,
      name,
      location,
      html_url: githubUrl,
      blog: blogUrl,
      bio,
      public_repos: repos,
      followers,
      public_gists: stars,
      following
    } = data;
    return ``;
  }
  
  module.exports = createNewDoc;